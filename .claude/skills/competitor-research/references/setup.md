# אשף ההקמה — `business-context.md` (משותף) + `competitors.md` + טוקן Apify

רץ פעם אחת, בריצה הראשונה. שלושה חלקים, לפי הסדר.

## 1. טוקן Apify (חוסם — בלעדיו אין נתונים)

בדוק אם `APIFY_TOKEN` קיים ב-`.env` ותקף (בדיקת החיבור ב-`references/apify.md`).

אם לא — **לעולם אל תבקש מהמשתמש להדביק את הטוקן בצ'אט.** במקום זה:

1. צור `.env` עם השורה `APIFY_TOKEN=` (אם אינו קיים) וודא שהוא ב-`.gitignore`.
2. **פתח את הקובץ למשתמש בעורך** (`open .env` במק / `notepad .env` בווינדוס / `code .env` אם יש VS Code).
3. אמור: "פתחתי לך את הקובץ. הדבק את הטוקן אחרי סימן השווה, שמור, ותגיד לי *מוכן*.
   (הטוקן: apify.com → Settings → Integrations → Personal API token. חשבון חינמי מספיק.)"
4. כשהמשתמש מאשר — קרא את הקובץ והרץ את בדיקת החיבור. אל תציג את ערך הטוקן חזרה בצ'אט.

אם המשתמש בכל זאת הדביק טוקן בצ'אט — שמור אותו ל-`.env` והמשך, אבל הסבר במשפט
שלהבא עדיף בקובץ, כי מה שנכתב בצ'אט נשאר בהיסטוריית השיחה.

## 2. העסק — קובץ אחד לכל הכלים

**סדר החיפוש:** `business-context.md` קיים בתיקייה → קרא ממנו והצג סיכום לאישור.
אין → שאב מאתר/מיפוי העסק של המועדון. אין כלום → שאל ובנה.

מ-`business-context.md` בדרך כלל כבר יש: מה העסק מוכר, למי, ואיפה.
**שאל רק את מה שחסר שם**, ואת התשובות **הוסף לאותו קובץ** (לא ליצור קובץ עסק חדש):

1. **מה העסק מוכר, במשפט אחד?** (אם חסר)
2. **מי הלקוח הטיפוסי?** (אם חסר)
3. **מה אזור השירות?** (קובע את רדיוס חיפוש המתחרים)
4. **מה הפרופילים שלך ברשתות?** (הפרופיל שלך נכנס להשוואה כשורה הראשונה.
   "אין לי" / "דלג" זו תשובה תקינה — הריצה תהיה ניתוח מתחרים בלי שורת השוואה אישית)
5. **באיזו רשת להתמקד?** ברירת מחדל: אינסטגרם. אפשר להוסיף טיקטוק/פייסבוק.

שאלות 4–5 נשמרות ב-`competitors.md` (הן הגדרת סריקה, לא זהות עסקית).

## 3. המתחרים

**מי המתחרים שאתה כבר מכיר?** שמות עסק או פרופילים. אפשר "לא יודע — תמצא":
חיפוש רשת לפי תחום+אזור → הצגת 5–8 מועמדים עם הפרופיל שאותר → המשתמש מסמן.
לכל מתחרה מאושר: משיכת פרופיל בודדת לאימות (קיים? ציבורי? זה באמת הם?).

## הפלט

### `business-context.md` (משותף לכל הכלים — נערך, לא נוצר מחדש)

אם לא היה קיים — צור אותו מינימלי: שם העסק, מה מוכר, לקוח טיפוסי, אזור.
אם היה קיים — הוסף/עדכן רק שדות חסרים. **אל תדרוס שום דבר קיים.**

### `competitors.md` (הקובץ של הסקיל הזה)

```markdown
# מחקר מתחרים — הגדרות ומעקב
- הפרופילים שלי: אינסטגרם @... · טיקטוק @...
- רשתות לסריקה: אינסטגרם

## מתחרים במעקב
| שם | אינסטגרם | עוקבים (עדכון אחרון) | נסרק לאחרונה |
|---|---|---|---|
```

הקבצים טקסט פשוט — המשתמש עורך ביד.
בסוף: "מחוברים ומוכנים. תגיד לי *תריץ מחקר מתחרים* ונצא לדרך."
